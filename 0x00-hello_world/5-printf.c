#include <stdio.h>

/**
 * main - print characters to standard output
 * Description: use the printf function to print characters to standard output
 * Return: return 0 on success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
